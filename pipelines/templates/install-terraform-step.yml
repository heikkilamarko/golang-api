parameters:
  - name: version
    type: string
    default: 0.13.0

steps:
  - task: terraformInstaller@0
    inputs:
        terraformVersion: ${{ parameters.version }}
    displayName: Install Terraform
